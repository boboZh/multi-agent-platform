This is NOT the Next.js you know
This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in node_modules/next/dist/docs/ before writing any code. Heed deprecation notices.

1、关于数据库表结构
supabase的数据表暂时不开启RSL，所有的user_id使用process.env.NEXT_PUBLIC_MOCK_USER_ID代替

2、关于代码注释
不要解释显而易见的废话（比如‘这里定义了一个变量’）。
必须解释业务逻辑和设计意图（例如：为什么这里要这样处理数据结构？这样做是如何避免画布意外重渲染的？这个 Zod 规则是为了防范什么脏数据？）。
在复杂的核心函数上方，写明这段代码的入参、出参和大致执行步骤。

3、关于单元测试：请使用 Vitest 为这段核心逻辑生成单元测试。
只针对纯函数和数据结构转换进行测试，绝对不要引入 React、DOM 或任何 UI 层的测试逻辑。
必须包含至少 3 个极端边界情况（Edge Cases）的测试用例（例如：必填字段缺失、传入非法类型、空数组等）。
用例的 it 描述请使用清晰的中文，说明该用例的具体测试意图。

4、关于文件目录
单个模块的组件，放在该模块的components目录下